// Clara global JS – initial stub
window.Clara = window.Clara || {};

(function () {
  document.addEventListener("DOMContentLoaded", function () {
    console.log("Clara global JS loaded");
    // Clara.initGlobal && Clara.initGlobal(); // we'll define this later
  });
})();

